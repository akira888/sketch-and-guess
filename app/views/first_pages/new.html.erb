<div class="card">
  <p>
    あなたのお題カードはこちら
  </p>
  <%= tag.ul class: "prompts" do %>
    <% @card.prompts.pluck(:word).each do |word| %>
      <%= tag.li word %>
    <% end %>
  <% end %>
  <% if @current_user.can_redraw? %>
    <%= button_to "カードを引き直す(3回まで)", redraw_card_user_path(@current_user), method: :patch %>
  <% end %>
  <% unless @current_user.card_decided? %>
    <%= button_to "これでOK", decide_card_user_path(@current_user), method: :patch %>
  <% end %>
</div>
<pre>
  todo:
    揃ったらsaikoro を振る
  するとお題が決まる

</pre>
